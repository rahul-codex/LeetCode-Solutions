  class Solution {
public:
    bool isPalindrome(int x) {
        if (x < 0) return false; 
        long ans = 0;
        int m = x;
        while (x > 0) {
            int lst = x % 10;
            ans = ans * 10 + lst;
            x = x / 10;
        }
        return m == ans; 
    }
};
