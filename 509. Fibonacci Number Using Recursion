class Solution {
public:
    int fib(int n) {
        if (n == 0 || n == 1)                        // this is base case 
            return n;

        return fib(n - 1) + fib(n - 2);              
    }
};
